//crear una función de flecha

const calcArea = radius => {
    //función regular
    return 3.14 * radius**2;
};
//guradamos la llamada de la funcion calcArea
const area = calcArea(5);
console.log('area');